aus = 14
rsa = 10 * aus
print(c(aus,rsa))

# Ctrl + Shift + C to comment lies
# Use = or <- to assign value
# Run to run command
# Clear Environment variables using rm()
# To delete all use rm(list=ls())
# Save data to a variable using save(a, file="sess1.Rdata")
# Save full Workspace using save(list=ls(all.names), file='sess1.Rdata') or save.image()
# Load a variable load(file="sess1.Rdata")

